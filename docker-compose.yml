services:
  redis:
    image: redis:7-alpine
    container_name: exporttrack-redis
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis-data:/data
    restart: unless-stopped

volumes:
  redis-data:
